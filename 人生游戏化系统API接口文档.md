# 人生游戏化系统 API 接口文档

## 基础说明

所有接口基础路径：`/api`
通用返回格式 `Result<T>`：

```json
{
  "code": 200,          // 200:成功, 500:失败, 401:未授权等
  "message": "success", // 提示信息
  "data": {}            // 核心业务数据体
}

1. 状态面板模块 (Player)

------------------

### 1.1 获取玩家整体状态

* **接口路径**：`/player/stats`

* **请求方式**：`GET`

* **返回数据 (`data`部分)**：

JSON
    {
      "level": 24,
      "exp": 3650,
      "maxExp": 5000,
      "hp": 85,
      "maxHp": 100,
      "mp": 60,
      "maxMp": 100,
      "attributes": [
        { "name": "力量 (STR)", "value": 45, "max": 100 },
        // ... 其他5个属性
      ]
    }

2. 技能树模块 (Skill)

----------------

### 2.1 获取技能列表

* **接口路径**：`/skill/list`

* **请求方式**：`GET`

### 2.2 新增技能

* **接口路径**：`/skill/add`

* **请求方式**：`POST`

* **请求参数 (Body-JSON)**：`{ "name": "新技能", "quality": "4", "level": 1 }`

### 2.3 修改技能

* **接口路径**：`/skill/update`

* **请求方式**：`PUT`

* **请求参数 (Body-JSON)**：`{ "id": 1, "name": "修改后的技能", "quality": "4", "level": 2 }`

### 2.4 遗忘(删除)技能

* **接口路径**：`/skill/delete/{id}`

* **请求方式**：`DELETE`
3. 任务模块 (Task)

--------------

### 3.1 获取任务列表

* **接口路径**：`/task/list`

* **请求方式**：`GET`

### 3.2 接取新任务

* **接口路径**：`/task/add`

* **请求方式**：`POST`

* **请求参数 (Body-JSON)**：`{ "title": "任务名称", "status": "未开始" }`

### 3.3 完成任务

* **接口路径**：`/task/complete/{id}`

* **请求方式**：`PUT`

* **说明**：后端需在此接口内处理玩家获得 EXP 的逻辑。
4. 修炼记录与番茄钟模块 (Record & Pomodoro)

---------------------------------

### 4.1 获取本周所有修炼记录

* **接口路径**：`/record/list`

* **请求方式**：`GET`

### 4.2 手动录入记录

* **接口路径**：`/record/add`

* **请求方式**：`POST`

* **请求参数 (Body-JSON)**：`{ "date": "周一", "duration": 120, "skillId": 1 }`

* **说明**：后端需同步增加对应 skillId 的技能 EXP，并判断技能是否升级。

### 4.3 专注打坐完成 (番茄钟结算)

* **接口路径**：`/pomodoro/complete`

* **请求方式**：`POST`

* **请求参数 (Body-JSON)**：`{ "duration": 25 }`

* **说明**：后端需扣除玩家 MP，增加玩家 EXP，如果升级，返回的数据结构中应携带提示 `{"upgradeMsg": "恭喜升级到Lv.25"}`。

### 4.4 删除记录

* **接口路径**：`/record/delete/{id}`

* **请求方式**：`DELETE`
